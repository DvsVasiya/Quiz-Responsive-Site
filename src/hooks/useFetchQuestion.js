import React from 'react'
import { useSelector } from 'react-redux'

const useTest = (id) => {


    const allSelectedQuestions =  useSelector((state) => state)  

    console.log(allSelectedQuestions)


    return data
}

 export default useTest